# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules
dist
dist-ssr
*.local

# Electron / Build
build/
win-unpacked/
*.exe
*.blockmap
release/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Offline background-removal model, copied out of node_modules by
# scripts/sync-bg-model.mjs before every build. ~85 MB of binary that npm
# already pins, so it is derived rather than tracked.
public/bg-removal/
